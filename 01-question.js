// === Program to Add Two Numbers === //

const num1 = 5;
const num2 = 25;

// add two numbers
const sum = num1 + num2;

// display the sum 
console.log(`The sum of ${num1} and ${num2} is: ${sum}`); //The sum of 5 and 25 is: 30

const prompt = require('prompt-sync')();
const num3 = parseInt(prompt('Enter the first number '));
const num4 = parseInt(prompt('Enter the second number '));

//add two numbers
const sum1 = num3 + num4;

// display the sum
console.log(`The sum of ${num3} and ${num4} is ${sum1}`);